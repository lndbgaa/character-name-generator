{
  "name": "server",
  "version": "1.0.0",
  "main": "./dist/app.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc && tsc-alias && cp -r src/templates dist/templates",
    "start": "node ./dist/app.js",
    "preview": "npm run build && npm run start",
    "dev": "tsx watch ./src/app.ts",
    "debug": "tsx watch --inspect ./src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "tsx": "^4.20.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.8.3"
  }
}
